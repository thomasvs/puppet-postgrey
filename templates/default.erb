# postgrey startup options, created for Debian

# WARNING - This file is managed by puppet, do not edit!

POSTGREY_OPTS="--inet=<%= scope.lookupvar("postgrey::host") %>:<%= scope.lookupvar("postgrey::port") %> --delay=<%= scope.lookupvar("postgrey::delay") %> --max-age=<%= scope.lookupvar("postgrey::max_age") %> --auto-whitelist-clients=<%= scope.lookupvar("postgrey::auto_whitelist_clients") %> --retry-window=<%= scope.lookupvar("postgrey::retry_window") %> <%= scope.lookupvar("postgrey::extra_options") %>"

# the --greylist-text commandline argument can not be easily passed through
# POSTGREY_OPTS when it contains spaces.  So, insert your text here:
#POSTGREY_TEXT="Your customized rejection message here"


